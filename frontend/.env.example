# Leave unset to call the API via the dev server's same-origin /api proxy (see vite.config.ts).
# Set this only if the backend is served from a different host/port than the frontend.
VITE_API_BASE_URL=
